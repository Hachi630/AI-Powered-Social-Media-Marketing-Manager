# Melo 项目技术栈详细说明

## 📋 目录

- [项目架构概览](#项目架构概览)
- [前端技术栈](#前端技术栈)
- [后端技术栈](#后端技术栈)
- [数据库](#数据库)
- [第三方服务与 API](#第三方服务与-api)
- [开发工具与配置](#开发工具与配置)
- [部署相关](#部署相关)
- [项目结构](#项目结构)

---

## 🏗️ 项目架构概览

**架构模式：** 前后端分离（SPA + RESTful API）

```
┌─────────────────┐         ┌─────────────────┐
│   Frontend       │         │    Backend      │
│   (React SPA)    │ ◄─────► │  (Express API)  │
│   Port: 3000     │  HTTP   │   Port: 5000   │
└─────────────────┘         └─────────────────┘
                                      │
                                      ▼
                              ┌─────────────────┐
                              │    MongoDB      │
                              │   (Database)    │
                              └─────────────────┘
```

---

## 🎨 前端技术栈

### 核心框架

#### **React 18.3.1**
- **用途：** UI 框架，构建用户界面
- **特性：**
  - React Hooks（useState, useEffect, useCallback, useMemo 等）
  - 函数式组件
  - Context API（主题管理、应用设置）
  - StrictMode（开发模式严格检查）
- **版本：** `^18.3.1`

#### **React DOM 18.3.1**
- **用途：** React 的 DOM 渲染库
- **特性：** 使用 `createRoot` API（React 18 新特性）

### 构建工具

#### **Vite 6.0.1**
- **用途：** 现代化前端构建工具和开发服务器
- **特性：**
  - 极速的 HMR（热模块替换）
  - 基于 ES modules 的构建
  - 开发服务器代理配置（API 请求代理到后端）
  - 生产环境代码压缩（esbuild）
- **配置：**
  - 开发端口：`3000`
  - 代理配置：`/api`, `/linkedin`, `/uploads` → `http://localhost:5000`
  - 构建输出：`dist/`
  - 源码映射：关闭（生产环境）

#### **TypeScript 5.6.2**
- **用途：** 类型安全的 JavaScript 超集
- **配置：**
  - 目标：`ES2020`
  - 模块：`ESNext`
  - JSX：`react-jsx`
  - 严格模式：启用
  - 模块解析：`bundler`（Vite 模式）

### UI 组件库

#### **Ant Design 6.0.0**
- **用途：** 企业级 React UI 组件库
- **使用组件：**
  - Layout（布局）
  - Button（按钮）
  - Modal（对话框）
  - Form（表单）
  - Input, TextArea（输入框）
  - Select, DatePicker, TimePicker（选择器）
  - Calendar（日历）
  - Table（表格）
  - Card（卡片）
  - Tabs（标签页）
  - Upload（上传）
  - Message（消息提示）
  - Tag（标签）
  - Typography（排版）
  - Grid（栅格系统）
  - Segmented（分段控制器）
  - Descriptions（描述列表）
  - Dropdown（下拉菜单）
  - App（应用容器，用于 message/notification）
- **样式：** 使用 `antd/dist/reset.css` 重置样式

### 路由

#### **React Router DOM 6.30.2**
- **用途：** 客户端路由管理
- **特性：**
  - 声明式路由
  - 嵌套路由
  - 路由守卫（通过组件状态）
  - 动态路由参数
- **使用：**
  - `BrowserRouter`（HTML5 历史模式）
  - `Routes`, `Route`
  - `useNavigate`, `useLocation`, `useParams`

### 状态管理

#### **React Context API**
- **用途：** 全局状态管理
- **实现：**
  - `ThemeContext`：主题管理（深色/浅色模式）
  - `AppSettingsContext`：应用设置管理
- **配合使用：**
  - `localStorage`：持久化存储（文件夹展开状态、选中对话等）

### 样式方案

#### **CSS Modules**
- **用途：** 组件级样式隔离
- **文件格式：** `*.module.css`
- **使用示例：**
  ```typescript
  import styles from './Component.module.css'
  <div className={styles.container}>
  ```

#### **Ant Design CSS**
- **用途：** 组件库默认样式
- **导入：** `import 'antd/dist/reset.css'`

#### **全局 CSS**
- **文件：** `index.css`, `App.css`
- **用途：** 全局样式、CSS 变量、深色模式样式

### 拖拽功能

#### **@dnd-kit/core 6.3.1**
- **用途：** 拖拽功能核心库
- **使用场景：** 日历项拖拽排序和移动
- **特性：**
  - 无障碍支持
  - 触摸设备支持
  - 自定义拖拽行为

#### **@dnd-kit/sortable 10.0.0**
- **用途：** 可排序列表功能
- **配合：** `@dnd-kit/core` 使用

### 日期处理

#### **dayjs 1.11.19**
- **用途：** 轻量级日期处理库
- **功能：**
  - 日期格式化
  - 日期计算
  - 时区处理
  - 相对时间显示
- **替代：** Moment.js（更轻量）

### 动画库

#### **framer-motion 12.23.26**
- **用途：** React 动画库
- **特性：**
  - 声明式动画
  - 手势支持
  - 布局动画

### Markdown 渲染

#### **react-markdown 10.1.0**
- **用途：** Markdown 内容渲染
- **配合：** `remark-gfm 4.0.1`（GitHub Flavored Markdown 支持）
- **使用场景：** AI 聊天回复中的 Markdown 格式内容

### 图标库

#### **lucide-react 0.561.0**
- **用途：** 现代图标库
- **特性：** SVG 图标，可自定义大小和颜色

#### **@ant-design/icons**
- **用途：** Ant Design 官方图标库
- **使用：** 与 Ant Design 组件配合使用

### 其他工具库

#### **jsonwebtoken 9.0.2**
- **用途：** JWT Token 解析（前端验证）
- **注意：** 主要用于客户端验证，实际认证在后端

#### **react-darkreader 1.5.6**
- **用途：** 深色模式切换（备选方案）
- **状态：** 可能已替换为自定义实现

#### **dotenv 17.2.3**
- **用途：** 环境变量管理（前端）
- **使用：** 读取 `VITE_*` 前缀的环境变量

### 开发依赖

#### **测试框架**
- **Vitest 4.0.16**：Vite 原生测试框架
- **@testing-library/react 16.3.1**：React 组件测试
- **@testing-library/jest-dom 6.9.1**：DOM 断言扩展
- **jsdom 27.3.0**：DOM 环境模拟

#### **代码质量**
- **ESLint 9.17.0**：代码检查
- **TypeScript ESLint 8.18.1**：TypeScript 代码检查
- **eslint-plugin-react-hooks 5.1.0**：React Hooks 规则
- **eslint-plugin-react-refresh 0.4.16**：React Fast Refresh 支持

#### **类型定义**
- **@types/react 18.3.12**：React 类型定义
- **@types/react-dom 18.3.1**：React DOM 类型定义
- **@types/node 24.10.1**：Node.js 类型定义
- **@types/jsonwebtoken 9.0.10**：JWT 类型定义
- **@types/mongoose 5.11.97**：Mongoose 类型定义（前端可能用于类型共享）

---

## ⚙️ 后端技术栈

### 核心框架

#### **Node.js**
- **版本要求：** v20.x 或更高（推荐），v18.16.1 最低
- **原因：** `pdf-parse` 库需要 Node.js 20+ 以获得最佳兼容性

#### **Express.js 4.21.1**
- **用途：** Web 应用框架
- **特性：**
  - RESTful API 路由
  - 中间件支持
  - 静态文件服务
  - 请求体解析（JSON, URL-encoded）
- **配置：**
  - 端口：`5000`（可通过 `PORT` 环境变量配置）
  - 请求体大小限制：`250MB`（支持视频上传）

### 运行时工具

#### **tsx 4.19.2**
- **用途：** TypeScript 直接执行工具
- **功能：**
  - 无需编译即可运行 TypeScript
  - 支持 `watch` 模式（热重载）
- **使用：** `npm run dev` → `tsx watch src/index.ts`

### 数据库

#### **MongoDB**
- **用途：** NoSQL 文档数据库
- **连接方式：** MongoDB Atlas（云数据库）或本地 MongoDB
- **连接字符串：** 通过 `MONGODB_URI` 环境变量配置

#### **Mongoose 9.0.0**
- **用途：** MongoDB 对象建模工具（ODM）
- **功能：**
  - Schema 定义
  - 数据验证
  - 中间件（pre/post hooks）
  - 查询构建器
  - 关联（populate）
- **模型：**
  - User（用户）
  - Conversation（对话）
  - CalendarItem（日历项）
  - Campaign（活动）
  - ProjectFolder（项目文件夹）
  - TwitterToken（Twitter 令牌）
  - TwitterRequestToken（Twitter 请求令牌）
  - LinkedInToken（LinkedIn 令牌）

### 认证与安全

#### **jsonwebtoken 9.0.2**
- **用途：** JWT Token 生成和验证
- **功能：**
  - 用户认证
  - Token 签名和验证
  - Token 过期管理

#### **google-auth-library 10.5.0**
- **用途：** Google OAuth 认证
- **功能：**
  - Google ID Token 验证
  - OAuth 2.0 流程处理

#### **cors 2.8.5**
- **用途：** 跨域资源共享中间件
- **配置：**
  - 开发环境：允许所有来源
  - 生产环境：仅允许配置的来源
  - 支持凭证（credentials）

### 文件处理

#### **multer 2.0.2**
- **用途：** 文件上传中间件
- **功能：**
  - Multipart/form-data 解析
  - 文件大小限制
  - 文件类型验证
- **使用场景：** 图片上传

#### **mammoth 1.11.0**
- **用途：** Word 文档（.docx）解析
- **功能：** 将 Word 文档转换为 HTML/文本

#### **pdf-parse 2.4.5**
- **用途：** PDF 文件解析
- **功能：** 提取 PDF 文本内容
- **注意：** 需要 Node.js 20+ 和 `@napi-rs/canvas` 依赖

### HTTP 客户端

#### **axios 1.13.2**
- **用途：** HTTP 请求库
- **使用场景：**
  - 调用第三方 API（Twitter, LinkedIn, Facebook, Instagram）
  - 下载图片
  - 文件上传到第三方服务

### 任务调度

#### **node-cron 3.0.3**
- **用途：** 定时任务调度
- **功能：**
  - Cron 表达式支持
  - 定时执行任务
- **使用场景：**
  - 每 5 分钟检查并发布待发布的 LinkedIn 内容
  - Cron 表达式：`*/5 * * * *`（每 5 分钟）

### 社交媒体 API 客户端

#### **twitter-api-v2 1.28.0**
- **用途：** Twitter/X API v2 客户端
- **功能：**
  - OAuth 1.0a 认证
  - 推文发布
  - 用户信息获取

### 环境变量管理

#### **dotenv 17.2.3**
- **用途：** 环境变量加载
- **功能：** 从 `.env` 文件加载环境变量到 `process.env`

### 日期处理

#### **dayjs 1.11.10**
- **用途：** 日期处理（与前端一致）
- **功能：** 日期格式化、计算、时区处理

### Markdown 处理

#### **react-markdown 10.1.0** + **remark-gfm 4.0.1**
- **用途：** Markdown 内容处理（后端可能用于内容转换）

### 查询字符串解析

#### **qs 6.14.0**
- **用途：** 查询字符串解析和序列化
- **功能：** 处理复杂的查询参数

### 开发依赖

#### **测试框架**
- **Jest 30.2.0**：JavaScript 测试框架
- **ts-jest 29.4.6**：TypeScript 支持
- **supertest 7.1.4**：HTTP 断言库
- **cross-env 10.1.0**：跨平台环境变量设置

#### **类型定义**
- **@types/express 5.0.0**：Express 类型定义
- **@types/node 22.19.1**：Node.js 类型定义
- **@types/cors 2.8.17**：CORS 类型定义
- **@types/jsonwebtoken 9.0.10**：JWT 类型定义
- **@types/multer 2.0.0**：Multer 类型定义
- **@types/node-cron 3.0.11**：node-cron 类型定义
- **@types/jest 30.0.0**：Jest 类型定义
- **@types/supertest 6.0.3**：Supertest 类型定义
- **@types/qs 6.14.0**：qs 类型定义

#### **TypeScript**
- **TypeScript 5.6.2**：类型系统
- **配置：**
  - 目标：`ES2022`
  - 模块：`NodeNext`
  - 模块解析：`nodenext`
  - 严格模式：启用
  - 输出目录：`dist/`

---

## 🗄️ 数据库

### MongoDB

#### **数据库类型：** NoSQL 文档数据库

#### **连接配置：**
- **URI：** 通过 `MONGODB_URI` 环境变量配置
- **默认：** `mongodb://localhost:27017/melo`
- **生产：** MongoDB Atlas（云数据库）

#### **连接选项：**
- `serverSelectionTimeoutMS: 5000`（5 秒超时）
- `socketTimeoutMS: 45000`（45 秒无活动关闭）

#### **数据模型（Mongoose Schemas）：**

1. **User（用户）**
   - 邮箱、密码
   - 品牌信息（品牌名、行业、调性、目标受众、知识库）
   - 选中的对话 ID
   - 时间戳

2. **Conversation（对话）**
   - 用户 ID（关联）
   - 标题
   - 消息数组（角色、内容、图片、时间戳）
   - 文件夹 ID（关联）
   - 时间戳

3. **CalendarItem（日历项）**
   - 用户 ID（关联）
   - 活动 ID（关联，可选）
   - 平台（instagram_post, facebook, twitter, linkedin 等）
   - 日期、时间
   - 标题、内容
   - 图片 URL
   - 内容变体（variants）
   - 状态（draft, scheduled, published）
   - 时间戳

4. **Campaign（活动）**
   - 用户 ID（关联）
   - 名称
   - 描述
   - 时间戳

5. **ProjectFolder（项目文件夹）**
   - 用户 ID（关联）
   - 名称
   - 时间戳

6. **TwitterToken（Twitter 令牌）**
   - 用户 ID（关联）
   - OAuth Token 和 Secret
   - 时间戳

7. **TwitterRequestToken（Twitter 请求令牌）**
   - 临时 OAuth 请求令牌
   - 过期时间

8. **LinkedInToken（LinkedIn 令牌）**
   - 用户 ID（关联）
   - Access Token
   - Refresh Token
   - 过期时间
   - 时间戳

---

## 🌐 第三方服务与 API

### AI 服务

#### **Google Gemini API**
- **SDK：** `@google/genai 1.30.0`
- **用途：**
  - AI 聊天对话（基于品牌资料）
  - 图像生成
  - 内容计划生成
- **模型：**
  - 聊天模型：`gemini-2.5-flash`（可配置）
  - 图像模型：`gemini-2.5-flash-image`（可配置）
- **配置：** 通过 `GEMINI_API_KEY` 环境变量

### 社交媒体 API

#### **Twitter/X API**
- **SDK：** `twitter-api-v2 1.28.0`
- **认证：** OAuth 1.0a
- **功能：**
  - 账户连接
  - 推文发布
- **配置：**
  - `TWITTER_API_KEY`
  - `TWITTER_API_SECRET`
  - `TWITTER_CALLBACK_URL`

#### **LinkedIn API**
- **HTTP 客户端：** `axios`
- **认证：** OAuth 2.0
- **功能：**
  - 账户连接
  - 帖子发布（文字 + 图片）
  - 自动调度发布
- **配置：**
  - `LI_CLIENT_ID`
  - `LI_CLIENT_SECRET`
  - `LI_REDIRECT_URI`

#### **Facebook Graph API**
- **HTTP 客户端：** `axios`
- **认证：** OAuth 2.0
- **功能：**
  - Facebook Page 管理
  - 帖子发布
- **配置：**
  - `FACEBOOK_APP_ID`
  - `FACEBOOK_APP_SECRET`
  - `FACEBOOK_REDIRECT_URI`

#### **Instagram Graph API**
- **HTTP 客户端：** `axios`
- **认证：** OAuth 2.0（通过 Facebook）
- **功能：**
  - Instagram Business Account 检测
  - 内容发布（Post）
  - 长生命周期 Token 管理
- **配置：**
  - 使用 Facebook App 凭证
  - `INSTAGRAM_REDIRECT_URI`

### OAuth 服务

#### **Google OAuth 2.0**
- **SDK：** `google-auth-library 10.5.0`
- **功能：** 用户登录认证
- **配置：**
  - `GOOGLE_CLIENT_ID`
  - `GOOGLE_CLIENT_SECRET`（后端）

---

## 🛠️ 开发工具与配置

### 代码编辑器配置

#### **TypeScript 配置**

**前端（tsconfig.app.json）：**
- 目标：`ES2020`
- 模块：`ESNext`
- JSX：`react-jsx`
- 模块解析：`bundler`（Vite 模式）
- 严格模式：启用

**后端（tsconfig.json）：**
- 目标：`ES2022`
- 模块：`NodeNext`
- 模块解析：`nodenext`
- 严格模式：启用
- 输出目录：`dist/`
- 声明文件：生成（`.d.ts`）

### 代码质量工具

#### **ESLint**
- **前端：** ESLint 9.17.0
- **配置：** `eslint.config.js`
- **插件：**
  - `eslint-plugin-react-hooks`
  - `eslint-plugin-react-refresh`
  - `typescript-eslint`

### 构建配置

#### **Vite 配置（vite.config.ts）**
- **开发服务器：**
  - 端口：`3000`
  - 代理：`/api`, `/linkedin`, `/uploads` → `http://localhost:5000`
  - WebSocket 支持
- **构建：**
  - 输出目录：`dist/`
  - 压缩工具：`esbuild`
  - 源码映射：关闭（生产）

### 环境变量

#### **前端环境变量（VITE_* 前缀）**
- `VITE_API_URL`：后端 API URL（生产环境必需）
- `VITE_GOOGLE_CLIENT_ID`：Google OAuth Client ID
- `VITE_BACKEND_PORT`：后端端口（开发环境，默认 5000）

#### **后端环境变量**
- `PORT`：服务器端口（默认 5000）
- `MONGODB_URI`：MongoDB 连接字符串
- `JWT_SECRET`：JWT 签名密钥
- `JWT_EXPIRE`：JWT 过期时间（默认 7d）
- `GEMINI_API_KEY`：Google Gemini API 密钥
- `GEMINI_MODEL`：Gemini 模型名称
- `GEMINI_IMAGE_MODEL`：Gemini 图像模型名称
- `GOOGLE_CLIENT_ID`：Google OAuth Client ID
- `GOOGLE_CLIENT_SECRET`：Google OAuth Client Secret
- `TWITTER_API_KEY`：Twitter API Key
- `TWITTER_API_SECRET`：Twitter API Secret
- `TWITTER_CALLBACK_URL`：Twitter OAuth 回调 URL
- `LI_CLIENT_ID`：LinkedIn Client ID
- `LI_CLIENT_SECRET`：LinkedIn Client Secret
- `LI_REDIRECT_URI`：LinkedIn OAuth 重定向 URI
- `FACEBOOK_APP_ID`：Facebook App ID
- `FACEBOOK_APP_SECRET`：Facebook App Secret
- `FACEBOOK_REDIRECT_URI`：Facebook OAuth 重定向 URI
- `INSTAGRAM_REDIRECT_URI`：Instagram OAuth 重定向 URI
- `CLIENT_URL`：前端 URL
- `FRONTEND_URL`：前端 URL（别名）
- `BACKEND_URL`：后端 URL
- `NODE_ENV`：环境模式（development/production）

---

## 🚀 部署相关

### 前端部署

#### **Vercel**
- **配置文件：** `vercel.json`
- **构建命令：** `npm run build`
- **输出目录：** `dist/`
- **环境变量：** 需要在 Vercel 控制台配置

### 后端部署

#### **Render**
- **配置文件：** `render.yaml`
- **构建命令：** `npm run build`
- **启动命令：** `npm start`
- **环境变量：** 需要在 Render 控制台配置

### 静态文件服务

#### **文件上传存储**
- **当前：** 本地文件系统（`backend/uploads/`）
- **注意：** Render 的文件系统是临时的，重启后会丢失
- **建议：** 生产环境使用云存储（AWS S3, Cloudinary 等）

---

## 📁 项目结构

```
Melo/
├── frontend/                    # 前端应用
│   ├── src/
│   │   ├── components/         # React 组件
│   │   │   ├── *.tsx          # 组件文件
│   │   │   └── *.module.css   # 组件样式
│   │   ├── pages/             # 页面组件
│   │   ├── services/          # API 服务层
│   │   ├── contexts/          # React Context
│   │   ├── utils/             # 工具函数
│   │   ├── constants/         # 常量
│   │   ├── App.tsx            # 根组件
│   │   └── main.tsx           # 入口文件
│   ├── public/                # 静态资源
│   ├── index.html             # HTML 模板
│   ├── vite.config.ts        # Vite 配置
│   ├── tsconfig.json          # TypeScript 配置
│   └── package.json           # 依赖配置
│
├── backend/                    # 后端应用
│   ├── src/
│   │   ├── config/            # 配置文件
│   │   │   └── database.ts    # 数据库连接
│   │   ├── models/            # Mongoose 模型
│   │   ├── routes/            # Express 路由
│   │   ├── services/          # 业务逻辑服务
│   │   ├── middleware/        # Express 中间件
│   │   ├── utils/             # 工具函数
│   │   ├── types/             # TypeScript 类型
│   │   └── index.ts           # 入口文件
│   ├── uploads/               # 上传文件存储
│   ├── dist/                  # 编译输出
│   ├── tsconfig.json          # TypeScript 配置
│   └── package.json           # 依赖配置
│
└── README.md                   # 项目文档
```

---

## 🔄 数据流

### 前端 → 后端
1. **HTTP 请求：** Axios/Fetch API
2. **认证：** JWT Token（`Authorization: Bearer <token>`）
3. **数据格式：** JSON
4. **代理：** Vite 开发服务器代理到后端

### 后端 → 数据库
1. **ORM：** Mongoose
2. **查询：** Mongoose Query API
3. **验证：** Schema 验证
4. **关联：** populate()

### 后端 → 第三方 API
1. **HTTP 客户端：** Axios
2. **认证：** OAuth 2.0 / OAuth 1.0a
3. **Token 管理：** 存储在 MongoDB

---

## 📊 技术栈总结表

| 类别 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **前端框架** | React | 18.3.1 | UI 框架 |
| **构建工具** | Vite | 6.0.1 | 构建和开发服务器 |
| **UI 组件库** | Ant Design | 6.0.0 | 企业级 UI 组件 |
| **路由** | React Router | 6.30.2 | 客户端路由 |
| **拖拽** | @dnd-kit | 6.3.1 | 拖拽功能 |
| **日期处理** | dayjs | 1.11.19 | 日期处理 |
| **动画** | framer-motion | 12.23.26 | 动画效果 |
| **Markdown** | react-markdown | 10.1.0 | Markdown 渲染 |
| **类型系统** | TypeScript | 5.6.2 | 类型安全 |
| **后端框架** | Express | 4.21.1 | Web 框架 |
| **数据库** | MongoDB | - | NoSQL 数据库 |
| **ODM** | Mongoose | 9.0.0 | MongoDB 对象建模 |
| **认证** | JWT | 9.0.2 | 身份验证 |
| **文件上传** | multer | 2.0.2 | 文件上传处理 |
| **任务调度** | node-cron | 3.0.3 | 定时任务 |
| **HTTP 客户端** | axios | 1.13.2 | HTTP 请求 |
| **AI 服务** | Google Gemini | 1.30.0 | AI 功能 |
| **Twitter API** | twitter-api-v2 | 1.28.0 | Twitter 集成 |

---

**最后更新：** 2025-01-06  
**分支：** main

